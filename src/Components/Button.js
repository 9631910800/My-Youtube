import React from 'react'

const Button = ({name}) => {
  return (
    <div>
        <button className='bg-slate-100 rounded-lg m-2 px-4 py-2'>{name}</button>
      
    </div>
  )
}

export default Button
